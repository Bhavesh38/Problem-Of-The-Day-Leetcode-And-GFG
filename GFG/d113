class Solution{
public:
    int minimumNumber(int n,vector<int> &arr){
        // Code here
        int ans=0;
        for(int i=0; i<arr.size();i++){
            ans = __gcd(ans,arr[i]);
        }
        return ans;
    }
};